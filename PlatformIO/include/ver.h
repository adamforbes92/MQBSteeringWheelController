#pragma once

/*
  MFSW Controller — firmware version history
  ------------------------------------------
  Bump FW_VERSION in defs.h whenever a new entry is added here.

V1.00 - initial release
V1.01 - added OTA
V1.02 - firmware version broken out to the web UI (header badge + OTA tab);
        internal modules de-VAG-ified (mqb_tasks -> tasks, mqb_defs -> defs)
*/
